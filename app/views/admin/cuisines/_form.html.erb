<%= form_for(@cuisine,:url => set_cuisine_path(@cuisine), :html => {:multipart => true}) do |f| %>
  <% if @cuisine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cuisine.errors.count, "error") %> prohibited this cuisine from being saved:</h2>

      <ul>
      <% @cuisine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Cuisine Name" %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :photo, "Cuisine Photo"%>
    <%= f.file_field :photo %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :cols => 40, :rows => 5 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
