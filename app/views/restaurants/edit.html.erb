<div class="row">
  <div class="container">		
		<h1>Editing restaurant</h1>

		<%= render 'form' %>
		<% if can? :read, Restaurant %>  
			<%= link_to 'Show', [@school, @restaurant] %> |
			<%= link_to 'Back', school_restaurants_path(@school) %>
		<% end %>	
	</div>
</div>		
